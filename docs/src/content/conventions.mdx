# Conventions

## Gaps

`.gap(level)` on `$flex` and `$grid` uses gap levels, which grow exponentially, not linearly.

Formula is `Math.pow(2, level) * 0.25rem`.

```tsx
$flex.gap(1); // 0.5rem
$flex.gap(2); // 1rem
$flex.gap(3); // 2rem
$flex.gap(4); // 4rem
```

